**문제 24** (3점)

행렬곱이 교환법칙을 만족하지 않는 이유를 설명하시오.

**답**: 행렬은 공간을 변환하는 함수이므로, 적용 순서가 다르면 결과가 다릅니다. $AB$는 먼저 $B$를 적용하고 $A$를 적용하는 변환이고, $BA$는 반대 순서이므로 일반적으로 다른 결과를 가집니다.

**해설**: 예를 들어 회전과 확대를 순서대로 적용하는 것과 반대로 적용하는 것은 다른 결과를 만듭니다. 벡터 내적은 스칼라(숫자)이므로 교환법칙이 성립하지만, 행렬 곱셈은 변환의 합성이라 순서가 중요합니다.

